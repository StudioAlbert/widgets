# widgets

Widgets pédagogiques interactifs (HTML autonomes) utilisés en cours par Sébastien Albert — programmation de jeux & IA.

Déployés via **GitHub Pages** et embarqués dans le [portfolio](https://github.com/StudioAlbert/Portfolio) via `<iframe>`.

## Widgets

| Widget | URL |
|--------|-----|
| A* — Recherche de chemin | [`/_widgets/astar_widgets.html`](https://studioalbert.github.io/widgets/_widgets/astar_widgets.html) |
| Utility AI — Courbes de réponse | [`/_widgets/utility_ai.html`](https://studioalbert.github.io/widgets/_widgets/utility_ai.html) |

## Source

Ces fichiers sont une copie des widgets du vault privé `Knowledges` (`_widgets/`, `_templates/css/`).
Pour mettre à jour : recopier les fichiers source puis `git push` (Pages se redéploie automatiquement).

`.nojekyll` est présent pour que les dossiers préfixés `_` soient servis tels quels.
